# 나라장터 API 클라이언트 의존성
# Note: 이 프로젝트는 curl subprocess를 사용하므로 외부 패키지 의존성이 최소화됨

# 웹 애플리케이션 의존성
Flask==2.3.3
Flask-CORS==4.0.0
pandas==2.1.4
openpyxl==3.1.2

# 개발시 테스트용으로 사용했던 패키지들 (선택사항)
# requests==2.31.0
# urllib3==2.0.4